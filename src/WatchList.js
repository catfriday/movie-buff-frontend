import React from 'react'
import { Link }  from 'react-router-dom'


const SideBar = (props) => {
 console.log(props)
    return (
        <div>
            {/* <p>{props.currentUser.posted_movies}</p> */}
        </div>
    )
}

export default SideBar

